pipeline: from_scratch
training:
  unfreeze_epoch: 0
  lr_scheduler:
    milestones:
      - 10
      - 15
model:
  backbone: resnet18
  pretrained: false
  pooling: avg # `avg` - average pooling or `gwap` - global weighted average pooling